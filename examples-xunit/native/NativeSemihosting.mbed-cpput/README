Project template created by gen-stm32f407-GCC-project.sh mbed-none-sh
   mbed-none-sh ... creates a bare-metal project with mbed SDK, cpputest and ARM semihosting support.

Usage [app]:  make clean && make && sudo make deploy
Usage [test]: make test-clean && make test-deps && make test && sudo make check && make check-coverage

